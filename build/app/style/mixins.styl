font-mx(s, w=300, l = 1, c = black)
  font-size s
  font-weight w
  line-height l
  color c

cont-mx(d = flex, jc = space-between, w = 100%)
  display d
  justify-content jc
  max-width w

anim-mx(t = .2s)
  transition t linear all

bord-rad(b = 7px)
  border-radius b

center-tx()
  text-align center
